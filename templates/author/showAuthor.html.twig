<!DOCTYPE html>
<html>
<head>
    <title>Author Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            color: #333;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            padding: 20px;
            text-align: center;
        }

        h1 {
            color: #2c3e50;
            text-transform: uppercase;
        }

        img {
            border-radius: 50%;
            margin: 20px 0;
        }

        p {
            font-size: 1.9em;
            color: #black;
        }

        a {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #3498db;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        a:hover {
            background-color: #2980b9;
        }
    </style>
</head>
<body>

<div class="container">
    <h1>{{ author.username|upper }}</h1>
    <img src="{{ author.picture }}" alt="{{ author.username }}" width="200" height="200">
    <p>Email : {{ author.email }}</p>
    <p>Number of books : {{ author.nb_books }}</p>

    <a href="{{ path('list_authors') }}">Back to list</a>
</div>

</body>
</html>
